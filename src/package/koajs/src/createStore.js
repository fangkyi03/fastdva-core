export default function createStore(option) {
    const app = {
        _store:{},
        _model:{},
        model,
        dispatch,
    }
    return app

    function model(m) {
        
    }

    function dispatch(params) {
        
    }
}